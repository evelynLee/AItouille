// 引用 LIFF SDK 的頁面，頁面中的 lang 值
liff.getLanguage();

// LIFF SDK 的版本
liff.getVersion();

// 回傳是否由 LINE App 存取
liff.isInClient();

// 使用者是否登入 LINE 帳號
liff.isLoggedIn();

// 回傳使用者作業系統：ios、android、web
liff.getOS();

// 使用者的 LINE 版本
liff.getLineVersion();